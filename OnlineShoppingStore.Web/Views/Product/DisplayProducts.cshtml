@model IEnumerable<OnlineShoppingStore.Domain.Entities.Product>
<script src="~/Scripts/Jscripts/DisplayProduct.js"></script>
<div class="panel panel-default">
    <div class="panel panel-heading">Products</div>
    <div class="text-right">
        @Html.ActionLink("Add New Product","AddUpdateProduct","Product", new { productId = 0 }, new { @style = "color:#000aaa;" })
    </div>
    <div class="panel panel-body">
        <form if="frmProduct" method="post">

            @*@if (Model.Count() > 0)
            {*@
            <table id="tblCateogry" class="table">
                <thead class="thead-dark">
                    <tr>
                        <td scope="col">Sr. No</td>
                        <td scope="col">Image</td>
                        <td scope="col">Product</td>
                        <td scope="col">Category</td>
                        <td scope="col">Price</td>
                        <td scope="col">Featured</td>
                        <td scope="col">Action</td>
                    </tr>
                </thead>
                <tbody>
                    @foreach(var item in Model)
                    {
                        var a = 0;
                        <tr>
                            <td>a++</td>
                            <td>@item.ProductImage</td>
                            <td>@item.ProductName</td>
                            <td>@item.CategoryId</td>
                            <td>@item.Price</td>
                            <td>@(item.IsFeatured==true?"Yes":"No")</td>
                            <td>
                                @Html.ActionLink("Edit", "AddUpdateProduct", "Product", new { productId = item.ProductId}, new { @class = "btn btn-primary" })
                                <input type="button" value="Delete" id="Delete" class="btn btn-danger" onclick="fnDelete(@item.CategoryId);" />
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
            @*}
            else
            {
                        <br />
                        <br />
                        <div class="text-center">
                            <h1>No record exists</h1>
                        </div>
            }*@
            </form>
    </div>
</div>
